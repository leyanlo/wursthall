# Wursthall

Wursthall website

## Build Setup

```sh
# Install Ruby if not already installed

# Install gems
bundle install

# Initialize .env.rb
echo "ENV['SECRET']" > .env.rb

# Start local server on localhost:3000
./start.sh
```
